import React from "react";
import { Helmet } from "react-helmet";

const Beer: React.FC = () => (
  <div>
    <Helmet>
      <title>Beers - BrewMate</title>
    </Helmet>
    <p>Beers</p>
  </div>
);

export { Beer };
