import React from "react";
import { Helmet } from "react-helmet";

const Dashboard: React.FC = () => (
  <div>
    <Helmet>
      <title>Dashboard - BrewMate</title>
    </Helmet>
    <p>Dashboard</p>
  </div>
);

export { Dashboard };
